<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm py-2">
  <div class="container">
    <%# ロゴ部分：足跡とテキストを横並びに %>
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center text-decoration-none" do %>
      <span style="font-size: 1.5rem;" class="me-2">🐾</span>
      <span class="fw-bold" style="color: #7c2d12; letter-spacing: -0.02em;">
        Pet Care Sharing App
      </span>
    <% end %>

    <%# 右側のナビゲーション項目 %>
    <div class="navbar-nav ms-auto flex-row">
  <% if logged_in? %>
    <%= link_to "ログアウト", logout_path, data: { turbo_method: :delete }, class: "nav-link px-3 small text-muted" %>
  <% else %>
    <%= link_to "新規登録", signup_path, data: { turbo: false }, class: "nav-link px-3 small text-muted" %>
    <%= link_to "ログイン", login_path, data: { turbo: false }, class: "nav-link px-3 small text-muted font-weight-bold", style: "color: #ea580c !important;" %>
  <% end %>
</div>
  </div>
</nav>